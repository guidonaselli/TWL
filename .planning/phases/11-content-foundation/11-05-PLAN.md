---
phase: 11-content-foundation
plan: 05
type: content
wave: 5
depends_on: ["11-04"]
files_modified:
  - Content/Data/pets.json
autonomous: true

must_haves:
  truths:
    - "pets.json contains 50+ pets with full skill sets, evolution paths, and capture rules"
  artifacts:
    - path: "Content/Data/pets.json"
      provides: "Pet definitions, evolution trees, and utility roles"
      contains: "EvolutionIds"
---

<objective>
Expand `pets.json` to 50+ pets covering all elements, utility roles (combat, mount), and clear evolution trees.

Purpose: This satisfies CONT-03 and enables Phase 9 (Pet System Completion) by providing necessary data for AI, Rebirth, and Riding.
Output: Completed `pets.json` with capture rates, base stats, and skill arrays.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@Content/Data/pets.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Capturable and Mountable Pets</name>
  <files>
    Content/Data/pets.json
  </files>
  <action>
    1. Add capturable wild pets corresponding to monsters defined in `monsters.json` (e.g., wolves, slimes).
    2. Define capture rates, base attributes, and element types.
    3. Flag specific pets (e.g., Horses, Tigers) with Mountable utility properties for riding speed bonuses.
    4. Ensure IDs use the 1000+ range.
  </action>
  <verify>
    `dotnet test TWL.Tests/TWL.Tests.csproj --filter ContentValidationTests` passes.
  </verify>
  <done>
    Capturable and mountable pets are defined and validate against the schema.
  </done>
</task>

<task type="auto">
  <name>Task 2: Define Human/Quest Pets and Evolution Chains</name>
  <files>
    Content/Data/pets.json
  </files>
  <action>
    1. Add unique non-capturable humanoid/quest pets (e.g., story companions) with custom initial stats.
    2. Implement evolution chains (e.g., `Base -> Form1 -> Form2`) using `EvolutionIds` for pet rebirth mechanics.
    3. Assign 3-4 specific skills to each pet, referencing valid IDs from `skills.json`.
  </action>
  <verify>
    `dotnet test TWL.Tests/TWL.Tests.csproj --filter ContentValidationTests` passes.
  </verify>
  <done>
    Quest pets and evolution chains are configured and validated.
  </done>
</task>

</tasks>

<verification>
- [ ] `pets.json` contains 50+ unique entries.
- [ ] Mountable/Utility flags are properly set.
- [ ] Evolution chains (`EvolutionIds`) are logically linked without cycles.
- [ ] `ContentValidationTests` pass.
</verification>

<success_criteria>
- CONT-03 is fully implemented.
- The Pet System (Phase 9) and Rebirth System (Phase 6) have necessary data to function.
</success_criteria>

<output>
After completion, create `.planning/phases/11-content-foundation/11-05-SUMMARY.md`
</output>
