---
phase: 01-infrastructure-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-01-PLAN", "01-02-PLAN", "05-04-PLAN"]
files_modified:
  - TWL.Server/Persistence/Database/Entities/GuildEntity.cs
  - TWL.Server/Persistence/Database/Configurations/GuildConfiguration.cs
  - TWL.Server/Persistence/Database/GameDbContext.cs
  - TWL.Shared/Domain/Guilds/IGuildRepository.cs
  - TWL.Server/Persistence/Repositories/DbGuildRepository.cs
  - TWL.Server/Persistence/Repositories/Queries/GuildQueries.cs
  - TWL.Server/Simulation/Managers/GuildManager.cs
  - TWL.Server/Simulation/Managers/GuildStorageService.cs
  - TWL.Server/Simulation/Program.cs
autonomous: true

must_haves:
  truths:
    - "Guild state is persisted in PostgreSQL instead of memory"
    - "DbGuildRepository uses EF Core for writes and Dapper for reads"
    - "GuildManager and GuildStorageService delegate to DbGuildRepository for state mutation"
    - "Race conditions on bank storage are protected by row-level locking or optimistic concurrency via EF Core"
  artifacts:
    - path: "TWL.Server/Persistence/Repositories/DbGuildRepository.cs"
      provides: "Guild persistence using hybrid EF/Dapper pattern"
      contains: "class DbGuildRepository"
    - path: "TWL.Server/Persistence/Database/Entities/GuildEntity.cs"
      provides: "Guild EF Core entity with JSONB mapping for members and storage"
      contains: "class GuildEntity"
  key_links:
    - from: "TWL.Server/Simulation/Managers/GuildManager.cs"
      to: "TWL.Shared/Domain/Guilds/IGuildRepository.cs"
      via: "Dependency Injection"
      pattern: "_guildRepository.SaveAsync"
---

<objective>
Migrate the in-memory guild state (`GuildManager.cs`, `GuildStorageService.cs`) to PostgreSQL using the EF Core + Dapper hybrid pattern established in Phase 1.

Purpose: Fixes the critical GAP where server restarts wipe all guilds and bank items. Ensures guild operations (creation, ranks, storage deposits/withdrawals) are durable and transaction-safe.

Output: `DbGuildRepository`, EF Core `GuildEntity` and mapping configuration, updated `GameDbContext`, and refactored `GuildManager`/`GuildStorageService` to load/save state instead of relying purely on concurrent dictionaries.
</objective>

<execution_context>
@C:/Users/guido/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/guido/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@TWL.Server/TWL.Server.csproj
@TWL.Server/Simulation/Managers/GuildManager.cs
@TWL.Server/Simulation/Managers/GuildStorageService.cs
@TWL.Server/Persistence/Repositories/DbPlayerRepository.cs
@TWL.Server/Persistence/Database/GameDbContext.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Guild entity definitions and schema updatess for GameDbContext</name>
  <files>
    TWL.Server/Persistence/Database/Entities/GuildEntity.cs
    TWL.Server/Persistence/Database/Configurations/GuildConfiguration.cs
    TWL.Server/Persistence/Database/GameDbContext.cs
  </files>
  <action>
    1. Create `TWL.Server/Persistence/Database/Entities/GuildEntity.cs`:
       - Class `GuildEntity` with properties matching `Guild`:
         - `int GuildId` (PK)
         - `string Name`
         - `int LeaderId`
         - `string MemberIdsJson` (maps to `HashSet<int>`)
         - `string MemberRanksJson` (maps to `Dictionary<int, GuildRank>`)
         - `string MemberJoinDatesJson` (maps to `Dictionary<int, DateTimeOffset>`)
         - `string StorageItemsJson` (maps to `Dictionary<int, int>`)
         - `DateTime LastSaved`
         - `uint Version` (for EF Core optimistic concurrency - `xmin` in Postgres, optional but recommended for bank sync)
    
    2. Create `TWL.Server/Persistence/Database/Configurations/GuildConfiguration.cs`:
       - Implements `IEntityTypeConfiguration<GuildEntity>`
       - Maps to table `guilds`
       - All JSON string properties use `.HasColumnType("jsonb")` with default `"{}"` or `"[]"`.
       - `Name` is required, unique, and has max length 50.
       - Configure `Version` as concurrency token if using `xmin`: `.IsRowVersion()` (for PostgreSQL, this is conventionally mapped to the hidden `xmin` column: `.UseXminAsConcurrencyToken()`).

    3. Update `GameDbContext.cs`:
       - Add `DbSet<GuildEntity> Guilds { get; set; }`

    4. Generate EF Migration:
       - Run `dotnet ef migrations add AddGuildSchema --project TWL.Server --output-dir Persistence/Database/Migrations`
       - Verify migration uses snake_case and has `jsonb` columns.
  </action>
  <verify>
    Run `dotnet build TWL.Server/TWL.Server.csproj`. Check migration file exists. Run `dotnet ef database update --project TWL.Server` (if docker is up) to verify schema applying.
  </verify>
  <done>
    `GuildEntity` created, `GameDbContext` updated, migration generated correctly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement IGuildRepository and DbGuildRepository (EF/Dapper Hybrid)</name>
  <files>
    TWL.Shared/Domain/Guilds/IGuildRepository.cs
    TWL.Server/Persistence/Repositories/DbGuildRepository.cs
    TWL.Server/Persistence/Repositories/Queries/GuildQueries.cs
    TWL.Server/Simulation/Program.cs
  </files>
  <action>
    1. Create `TWL.Shared/Domain/Guilds/IGuildRepository.cs`:
       - `Task<Guild?> LoadAsync(int guildId);`
       - `Task<Guild?> LoadByNameAsync(string name);`
       - `Task<IEnumerable<Guild>> LoadAllAsync();` (for server startup caching if needed)
       - `Task<bool> SaveAsync(Guild guild);` (returns false on concurrency conflict)
       - `Task DeleteAsync(int guildId);`

    2. Create `TWL.Server/Persistence/Repositories/Queries/GuildQueries.cs`:
       - Static SQL strings for Dapper.
       - e.g. `LoadById` = `"SELECT * FROM guilds WHERE guild_id = @GuildId"`
       - e.g. `LoadAll` = `"SELECT * FROM guilds"`

    3. Create `TWL.Server/Persistence/Repositories/DbGuildRepository.cs`:
       - Inject `IDbContextFactory<GameDbContext>`, `IDapperService`, `ILogger`.
       - `SaveAsync` maps `Guild` fields and JSON-serializes the collections. Uses EF Core `SaveChangesAsync()`. Catch `DbUpdateConcurrencyException` and return false if handling concurrency.
       - `LoadAsync`, `LoadByNameAsync`, `LoadAllAsync` use Dapper cleanly, mapping JSONB `string` columns back to `HashSet`/`Dictionary` via `System.Text.Json.JsonSerializer`.

    4. Register in `Program.cs`:
       - `svcs.AddSingleton<IGuildRepository, DbGuildRepository>();`
  </action>
  <verify>
    Run `dotnet build TWL.Server/TWL.Server.csproj`. Code compiles cleanly.
  </verify>
  <done>
    Repository layer implemented using the same pattern as `DbPlayerRepository`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Refactor GuildManager and GuildStorageService to use the repository</name>
  <files>
    TWL.Server/Simulation/Managers/GuildManager.cs
    TWL.Server/Simulation/Managers/GuildStorageService.cs
  </files>
  <action>
    1. Update `GuildManager.cs`:
       - Inject `IGuildRepository`.
       - During initialization or lazy-load, load guilds from `_guildRepository.LoadAllAsync()`. Cache them in `_guilds` and `_playerGuildMap` OR query them specifically. Given server-authoritative structure, keeping the caching layer `ConcurrentDictionary` and writing through to the repository is best.
       - Add `await _guildRepository.SaveAsync(guild);` to:
         - `CreateGuild`
         - `AcceptInvite`
         - `LeaveGuild` (if last member, `_guildRepository.DeleteAsync(guildId)`)
         - `KickMember`
         - `PromoteMember`
         - `DemoteMember`
       - Note: Since existing methods are synchronous, either change them to `async Task<(bool, string)>` and flow `await` up the call chain (preferred) or use fire-and-forget save hooks (less safe). Change them to async.

    2. Update `GuildStorageService.cs`:
       - Inject `IGuildRepository`.
       - In `DepositItem` and `WithdrawItem`:
         - Modify to be `async Task<GuildStorageOperationResultEvent>`.
         - After modifying `guild.StorageItems`, call `await _guildRepository.SaveAsync(guild);`.
         - Since `guild.StorageItems` is shared state, ensure atomic updates or retry logic if `SaveAsync` detects concurrency exception. Alternatively, lock `guild` object, modify in memory, then `SaveAsync`. The `lock (guild)` block already exists, so simply awaiting `SaveAsync` inside the lock (if allowed by caller context) or right after the lock is sufficient. (Careful with `await` inside `lock` - C# doesn't allow it. Use `SemaphoreSlim` on the Guild object OR save outside the lock after taking a snapshot).
         - Example:
           ```csharp
           // Inside try-finally with SemaphoreSlim instead of lock(guild):
           // modify storage
           // await save
           ```

    3. Update any callers to `GuildManager` and `GuildStorageService` to await the new Task-based methods. This includes network packet handlers mapping to these events.
  </action>
  <verify>
    Run `dotnet build TWL.Server/TWL.Server.csproj`. Resolve all async conversion compilation errors. Run unit tests `dotnet test TWL.Tests/` to ensure no test breaks (update mocks if needed).
  </verify>
  <done>
    In-memory guild changes now durably write through to PostgreSQL via IGuildRepository.
  </done>
</task>

</tasks>

<verification>
1. `dotnet build TWL.Server/TWL.Server.csproj` passes.
2. `dotnet test TWL.Tests/` passes.
3. Review `TWL.Server/Persistence/Database/Migrations/` for the new Guild schema migration.
</verification>

<success_criteria>
- Guild rosters, ranks, join dates, and storage persist in PostgreSQL.
- Server restarts do not lose guild membership or shared bank items.
- `DbGuildRepository` implemented using EF Core + Dapper hybrid pattern.
- Concurrent bank deposits don't cause data loss.
</success_criteria>

<output>
After completion, verify tests and generate `.planning/phases/01-infrastructure-foundation/01-03-SUMMARY.md` documenting completion.
</output>
